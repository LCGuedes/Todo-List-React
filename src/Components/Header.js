import React from "react";
import { Header, H1 } from "../Styles/header";

const _Header = () => {
    return(
        <Header>
            <H1>
                Adicionar
            </H1>
        </Header>
    );
};

export default _Header;